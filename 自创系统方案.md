# 自创系统 — 完整设计方案

## 一、整体流程

```
玩家 → 与朱熹对话 → 购买"空白秘籍"(消耗黄金)
  → 命名(必须) → 变成 "xxx秘籍" lv0 物品
  → 推演:
      ① 选择推演部位 (内功/剑法/刀法/拳脚/棍法/杖法/鞭法/暗器/轻功/招架)
      ② 选择词条 (消耗武道书，每词条1本)
      ③ 选择PFM  (消耗武道书，从自身3000级技能中自选，最多3个)
  → 确认推演 → 词条数决定grade，生成自创技能
  → 学习 → 获得自创技能，可装备使用
  → 后续：消耗潜能升级词条(无限等级)，消耗武道书继续推演新增词条/PFM
```

---

## 二、新增/修改文件清单

### 服务端 (world/)

| 文件 | 动作 | 说明 |
|------|------|------|
| `world/npc/yz/zhu.js` | **新增** | 朱熹NPC，提供"购买空白秘籍"交互 |
| `world/obj/zc/blank_book.js` | **新增** | 空白秘籍物品，自创核心逻辑载体 |
| `world/obj/zc/wudao_book.js` | **新增** | 武道书物品（如已有则修改） |
| `world/cmd/skill/zc.js` | **新增** | `zc` 命令集 |
| `world/extends/char/user.js` | **修改** | `create_for` 增强；新增词条升级相关方法 |
| `os/skill/skill.js` | **修改** | 扩展 `SKILL.PROPERTIES` 词条池 |

### 前端 (src/)

| 文件 | 动作 | 说明 |
|------|------|------|
| `src/dialog/zc.js` | **新增** | 自创推演专用对话框 |
| `src/dialog/skills.js` | **修改** | 补充自创技能的命令（已有 `is_custom` 雏形在第64行） |

---

## 三、核心数据结构

### 3.1 空白秘籍

```javascript
// world/obj/zc/blank_book.js
{
    unit: "本",
    name: "空白秘籍",
    desc: "一本空白的秘籍，可推演自创武学。",
    grade: 0,
    value: 100000,               // 售价100两黄金
    otype: 1,                    // 可学习类型物品

    // 自创特有属性 (运行时挂载)
    zc_state: "blank",          // blank | named | deducing | completed
    zc_name: null,              // 玩家命名的名字，命名后变 "xxx秘籍"
    zc_positions: [],           // 已推演的部位列表，支持多部位
    zc_words: {},               // 按部位分组的词条: {"拳脚":[500,501], "轻功":[504]}
    zc_pfms: {},                // 按部位分组的PFM: {"拳脚":[{...}], "轻功":[{...}]}
    zc_skill_id: null,          // 推演后生成的技能 ID: "zc_" + uniqueId
    is_locked: false,
}
```

**重要**: 秘籍物品是自创系统的操作蓝本。命名、推演、词条升级、PFM选择等一切操作均通过秘籍物品进行。学习后获得的技能按普通技能方式显示和使用，不再包含词条升级等操作入口。

### 3.2 部位 → 基础技能映射

```javascript
const ZC_POSITIONS = {
    "内功":  { base: "force",     label: "内功" },
    "剑法":  { base: "sword",     label: "剑法" },
    "刀法":  { base: "blade",     label: "刀法" },
    "拳脚":  { base: "unarmed",   label: "拳脚" },
    "棍法":  { base: "club",      label: "棍法" },
    "杖法":  { base: "staff",     label: "杖法" },
    "鞭法":  { base: "whip",      label: "鞭法" },
    "暗器":  { base: "throwing",  label: "暗器" },
    "轻功":  { base: "dodge",     label: "轻功" },
    "招架":  { base: "parry",     label: "招架" },
};
```

词条的 `positions` 字段决定哪些部位可用。武器类部位归并为 `"weapon"`：
- `"unarmed"`: 拳脚
- `"weapon"`: 剑法、刀法、棍法、杖法、鞭法、暗器
- `"force"`: 内功
- `"dodge"`: 轻功
- `"parry"`: 招架

### 3.3 自创词条池

词条挂载在 `SKILL.PROPERTIES` 数组尾部（index 500+），**沿用自制（duanzao）的属性分类体系**（category 0-4），不同分类的成长速度不同。

#### 3.3.0 分类与成长规则

| 分类 | 类别名 | 说明 | 固定值公式 | 百分比divisor | 基础消耗 |
|------|--------|------|-----------|---------------|----------|
| **0** | 基础属性 | 攻防命闪招气血内力等核心属性 | `floor(lv/100+1)×(grade+1)×36×wl` | — | 100-150 |
| **1** | 后天属性 | 臂力/根骨/身法/悟性 | `(1+grade×2)×80×wl` | — | 200 |
| **2** | 高级属性 | 普通百分比属性 | — | **20**（最高成长） | 200 |
| **3** | 稀有属性 | 强力百分比属性 | — | **25**（中等成长） | 250 |
| **4** | 特殊属性 | 特殊机制属性 | — | **30~35**（最低成长） | 300 |

> 百分比 divisor 越小成长越快：高级(20) > 稀有(25) > 特殊(30~35)。这与自制系统中高级属性容易洗出、特殊属性极稀有的定位一致。

每个词条定义示例：

```javascript
// 基础属性 (category 0) — 固定值
{
    index: 500,
    name: "攻击",
    category: 0,
    positions: ["parry", "dodge", "unarmed", "weapon", "force"],
    prop: "gj",
    value_type: "flat",
    value: function(lv, word_level, grade) {
        var base = Math.floor(lv / 100 + 1) * (grade + 1) * 36;
        return Math.floor(base * (1 + word_level / 15));
    },
    format: function(val) { return "攻击+" + val; },
    base_pot_cost: 100,
    upgrade_cost: function(target_level) {
        return this.base_pot_cost * target_level;
    },
}

// 后天属性 (category 1) — 固定值
{
    index: 508,
    name: "身法",
    category: 1,
    positions: ["parry", "dodge", "unarmed", "weapon", "force"],
    prop: "dex",
    value_type: "flat",
    value: function(lv, word_level, grade) {
        var base = (1 + grade * 2) * 80;
        return Math.floor(base * (1 + word_level / 15));
    },
    format: function(val) { return "身法+" + val; },
    base_pot_cost: 200,
    upgrade_cost: function(target_level) {
        return this.base_pot_cost * target_level;
    },
}

// 高级属性 (category 2) — 百分比，divisor=20 成长最高
{
    index: 506,
    name: "气血上限%",
    category: 2,
    positions: ["parry", "dodge", "unarmed", "weapon", "force"],
    prop: "hp_per",
    value_type: "percent",
    divisor: 20,
    value: function(lv, word_level, grade) {
        return 1 + Math.floor(word_level * (1 + grade) / (this.divisor * 2));
    },
    format: function(val) { return "气血+" + val + "%"; },
    base_pot_cost: 200,
    upgrade_cost: function(target_level) {
        return this.base_pot_cost * target_level;
    },
}

// 稀有属性 (category 3) — 百分比，divisor=25 成长中等
{
    index: 513,
    name: "最终伤害%",
    category: 3,
    positions: ["unarmed", "weapon"],
    prop: "add_sh_per",
    value_type: "percent",
    divisor: 25,
    value: function(lv, word_level, grade) {
        return 1 + Math.floor(word_level * (1 + grade) / (this.divisor * 2));
    },
    format: function(val) { return "最终伤害+" + val + "%"; },
    base_pot_cost: 250,
    upgrade_cost: function(target_level) {
        return this.base_pot_cost * target_level;
    },
}

// 特殊属性 (category 4) — 百分比，divisor=30~35 成长最低
{
    index: 512,
    name: "暴击%",
    category: 4,
    positions: ["weapon", "unarmed"],
    prop: "bj_per",
    value_type: "percent",
    divisor: 30,
    value: function(lv, word_level, grade) {
        return 1 + Math.floor(word_level * (1 + grade) / (this.divisor * 2));
    },
    format: function(val) { return "暴击+" + val + "%"; },
    base_pot_cost: 300,
    upgrade_cost: function(target_level) {
        return this.base_pot_cost * target_level;
    },
}

// 特殊属性 (category 4) — 固定时间值
{
    index: 516,
    name: "攻速",
    category: 4,
    positions: ["unarmed", "weapon"],
    prop: "gjsd",
    value_type: "flat_time",
    multiplier: 2,                      // ms per (grade+1) per word_level
    value: function(lv, word_level, grade) {
        return Math.floor(word_level * (grade + 1) * this.multiplier / 3);
    },
    format: function(val) { return "攻速-" + (val / 1000).toFixed(1) + "秒"; },
    base_pot_cost: 300,
    upgrade_cost: function(target_level) {
        return this.base_pot_cost * target_level;
    },
}
```

词条列表示例（第一期 22 个，按分类排列）：

| index | 名称 | 分类 | 部位 | 属性 | 值类型 | 公式参数 | 基础消耗 |
|-------|------|------|------|------|--------|----------|----------|
| 500 | 攻击 | 0-基础 | ALL | gj | flat | — | 100 |
| 501 | 防御 | 0-基础 | ALL | fy | flat | — | 100 |
| 502 | 命中 | 0-基础 | ALL | mz | flat | — | 100 |
| 503 | 招架 | 0-基础 | ALL | zj | flat | — | 100 |
| 504 | 闪避 | 0-基础 | ALL | ds | flat | — | 100 |
| 505 | 气血上限 | 0-基础 | ALL | max_hp | flat | — | 150 |
| 506 | 内力上限 | 0-基础 | parry,force,unarmed,dodge | max_mp | flat | — | 150 |
| 507 | 身法 | 1-后天 | ALL | dex | flat | — | 200 |
| 508 | 悟性 | 1-后天 | ALL | int | flat | — | 200 |
| 509 | 根骨 | 1-后天 | ALL | con | flat | — | 200 |
| 510 | 臂力 | 1-后天 | ALL | str | flat | — | 200 |
| 511 | 气血上限% | 2-高级 | ALL | hp_per | percent | ÷20 | 200 |
| 512 | 最终伤害% | 3-稀有 | unarmed,weapon | add_sh_per | percent | ÷25 | 250 |
| 513 | 伤害减免% | 3-稀有 | parry,dodge,force,unarmed | diff_sh_per | percent | ÷25 | 250 |
| 514 | 攻速% | 3-稀有 | unarmed,weapon | gjsd_per | percent | ÷25 | 250 |
| 515 | 忽视防御% | 3-稀有 | unarmed,weapon | diff_fy_per | percent | ÷25 | 250 |
| 516 | 释放时间% | 3-稀有 | force | releasetime_per | percent | ÷25 | 250 |
| 517 | 暴击% | 4-特殊 | weapon,unarmed | bj_per | percent | ÷30 | 300 |
| 518 | 冷却% | 4-特殊 | force | distime_per | percent | ÷35 | 300 |
| 519 | 攻速 | 4-特殊 | unarmed,weapon | gjsd | flat_time | ×2ms | 300 |
| 520 | 冷却 | 4-特殊 | force | distime | flat_time | ×15ms | 300 |
| 521 | 释放时间 | 4-特殊 | force | releasetime | flat_time | ×10ms | 300 |
| 522 | 血杀之意 | 特殊被动 | force,unarmed,weapon | blood_fury | passive | ×0.05 | 500 |

> **ALL** = parry, dodge, unarmed, weapon, force（全部5个部位）
> 分类号对应自制(duanzao)系统：0=基础属性, 1=后天属性, 2=高级属性, 3=稀有属性, 4=特殊属性
> 血杀之意为特殊被动词条（详见 3.3.2），index 522 并入主表便于统一管理

#### 3.3.1 部位特殊规则

**全部位开放词条**：攻击、防御、命中、招架、闪避、气血上限、身法、悟性、根骨、臂力、气血上限%（index 500-511）对所有部位可用。

**内功部位**：

1. **自带属性**: 内功部位自创技能自动附带"内力百分比转化血量上限"属性，与正常内功技能一致。
2. **首词条锁定**: 内功部位第一个词条只能选择 **内力上限**（index 506）。
3. **高级补偿词条**: 作为上述限制的补偿，内功部位可额外选择以下4个词条：
   - 释放时间% — index 516（稀有，divisor=25）
   - 冷却% — index 518（特殊，divisor=35）
   - 冷却 / 固定值 — index 520（特殊）
   - 释放时间 / 固定值 — index 521（特殊）

**武器部位**（weapon = 剑法/刀法/棍法/杖法/鞭法/暗器）：

- **伤害减免%**（index 513）不可选。

**轻功/招架部位**（dodge/parry）：

- **攻速%**（index 514）不可选。
- **攻速**（index 519）不可选。

#### 3.3.2 推演被动特殊词条（可扩展接口）

被动词条（如血杀之意）已并入主词条表（index 522），与普通词条一起在推演阶段选择。其 `value_type: "passive"`，成长公式独立，同样支持潜能升级。

为方便后续扩展，保留独立的 `ZC_SPECIAL_WORDS` 结构作为添加新被动的入口：

```javascript
// 后续新增被动只需 push 到此数组，index 使用 600+ 号段（或继续在主表中递增）
const ZC_SPECIAL_WORDS = [
    // 第一批被动已并入主表（522: 血杀之意），此处为扩展预留
    // {
    //     index: 600,
    //     name: "新被动名",
    //     category: "passive",
    //     positions: [...],
    //     desc: "被动效果描述",
    //     effect_type: "passive",      // "passive" | "trigger" | "aura"
    //     trigger: "battle_start",     // effect_type ≠ "passive" 时指定触发时机
    //     prop: "custom_key",
    //     value: function(lv, word_level, grade) { ... },
    //     base_pot_cost: 500,
    //     upgrade_cost: function(target_level) {
    //         return this.base_pot_cost * target_level;
    //     },
    // },
];

// 推演时合并可选词条
function get_available_words(position) {
    var normal = filterWords(ZC_WORDS, position);         // 普通属性词条（含522血杀之意）
    var special = filterWords(ZC_SPECIAL_WORDS, position); // 未来特殊被动词条
    return { normal: normal, special: special };
}
```

**扩展方式**：后续向 `ZC_SPECIAL_WORDS` 数组（600+）或主表末尾追加定义即可。前端在推演界面中将特殊词条单独分区展示（如"被动"标签页），消耗武道书与普通词条一致（1本/个）。

### 3.4 自创技能 (推演完成后动态生成)

```javascript
// 运行时注册到 WORLD.SKILLS
{
    id: "zc_" + uniqueId,
    name: player_named_name,        // e.g. "降龙十八掌"
    type: SKILL_TYPES.SKILL,
    grade: zc_words.length,         // ⭐ 词条数决定 grade
    can_enables: [position.base],   // 由推演部位决定
    is_custom: true,
    creator: player.id,
    creator_name: player.name,
    source_skill: null,             // 自创技能无来源
    zc_position: position_key,      // 保留部位名

    // 内功部位自动附带内力百分比转化血量上限（同正常内功）
    mp_to_hp: position_key === "内功" ? true : false,

    // 词条
    addin_prototype: [...],         // 新技能初始 addin = zc_words
    slots: CUSTOM_WORD_SLOTS,       // 自定义词条定义列表

    // PFM
    pfm: {                          // 从玩家3000级技能中自选的PFM
        pfm1: { /* 复制的PFM定义 */ },
        pfm2: { /* 复制的PFM定义 */ },
        pfm3: { /* 复制的PFM定义 */ },
    },

    desc: "xxx所创造的武功",
    color_name: "<cc>降龙十八掌</cc>",
}
```

### 3.5 玩家身上存储

```javascript
// USER 扩展属性
me.custom_skills = ["zc_abc123", "zc_def456"];  // 已有的自创技能ID列表

// 每个自创技能在 me.skills 中的存储
me.skills["zc_abc123"] = {
    level: 150,         // 技能等级
    exp: 50,            // 经验百分比
    enable_skill: null, // 装备到哪个基础技能
    // ⭐ 词条等级（核心）
    word_levels: [3, 1, 5, 2],  // 与 zc_words 一一对应，每个词条的升级等级
};
```

---

## 四、交互流程详解

### 4.1 购买空白秘籍

**NPC 文件**: `world/npc/yz/zhu.js`

```javascript
this.inherits(NPC);
this.set({
    name: "朱熹",
    desc: "一位儒雅的老者，虽非武林中人，却通晓天下武学之理。",
    gender: 1,
    age: 65,
});

this.on_enter = function(me) {
    // 暂时：百姓即可购买
    // 后续改为：me.level >= 武神阶段
    me.notify("朱熹手抚长须：这位" + me.call() + "，老夫这里有空白秘籍出售，可助你开创独门武学。");
    return me.send_commands(
        "buy blank_book from " + this.id, "购买空白秘籍"
    );
};
```

**购买判定**:
```javascript
function buy_blank_book(me) {
    // [暂] 无等级限制
    // [后续] if (me.level < WUSHEN_LEVEL) return me.notify_fail("...");
    if (me.money < 100000) return me.notify_fail("朱熹摇头：一百两黄金，不二价。");
    if (me.is_full()) return me.notify_fail("你身上的东西太多了。");
    me.money -= 100000;
    var book = WORLD.create_obj("zc/blank_book");
    me.push_item(book);
    me.notify("朱熹递给你一本" + book.name + "：望你自创出一门惊天动地的绝学。");
}
```

### 4.2 命名流程

**前提**: 秘籍处于 `blank` 状态

```
玩家点击[命名] → zc name <item_id>
  → 服务端: me.wait_input = read_zc_name
  → 前端弹出输入框: "请为你的秘籍命名（2-5个汉字）"
  → 玩家输入: "降龙十八掌"
  → 校验: 汉字2-5个，不重复，不违禁
  → 秘籍 name 变为 "降龙十八掌秘籍"
  → zc_state = "named"
  → zc_name = "降龙十八掌"
  → 服务端通知: "你为空白秘籍命名为《降龙十八掌秘籍》。"
```

**注意**: 命名后将无法回退到空白状态。如果玩家丢弃已命名的秘籍，需要重新购买空白秘籍。

### 4.3 推演流程（核心）

#### Step 1: 进入推演 — 选择部位

```
玩家点击[推演] → zc deduce <item_id>

前提: zc_state === "named" (必须先命名)

服务端返回部位列表:
{
    type: "dialog",
    dialog: "zc",
    step: "select_position",
    positions: ["内功","剑法","刀法","拳脚","棍法","杖法","鞭法","暗器","轻功","招架"],
    book_name: "降龙十八掌秘籍"
}

前端展示: 部位选择界面 (十选一)
```

#### Step 2: 选择词条

```
玩家点击某个部位 → zc select <item_id> <position>

服务端返回:
{
    step: "select_words",
    position: "拳脚",
    base_skill: "unarmed",
    available_words: [
        { index:500, name:"攻击", desc:"攻击+...", cost:1 },
        { index:501, name:"防御", desc:"防御+...", cost:1 },
        // ... 只返回该部位支持的词条
    ],
    selected_words: [],     // 当前已选词条
    wudao_count: 3,         // 背包中的武道书数量
    pfms_available: [...],  // 可选的PFM列表(见4.4)
    selected_pfms: [],      // 当前已选PFM
}

前端展示: 词条多选列表 + 武道书余额 + 实时预览已选效果
```

**验证**:
- 每个词条消耗 1 本武道书
- 词条数上限: 首次推演 6 个
- 部位必须匹配
- **内功部位**: 第一个词条必须为"内力上限"（index 506）

#### Step 3: 确认推演

```
玩家选好词条和PFM → zc confirm <item_id> <word_indices> <pfm_selections>

服务端:
1. 校验词条数 ≤ 6 且 > 0
2. 校验每个词条都支持当前部位
3. 校验PFM选择有效（见4.4）
4. 计算总消耗 = 词条数 + PFM消耗武道书之和
5. 校验武道书足够
6. 扣除武道书
7. 更新秘籍:
   - zc_words = [500, 503, 510, ...]
   - custom_grade = zc_words.length
   - zc_pfms = [...]
   - zc_state = "completed"
8. 动态创建 SKILL 对象并注册到 WORLD.SKILLS
9. zc_skill_id = "zc_" + uniqueId
10. 返回成功，秘籍 grade 变色

秘籍状态变化:
  命名后: lv0 白色物品 (grade=0)
  推演后: grade 变为词条数 (grade=1~6)，对应颜色变化
```

#### 4.3.1 多部位推演

一个玩家同一时间只能拥有 **一个** 自创技能，但可以对该技能进行**多部位推演**：

- 首次推演选择一个部位（如拳脚），完成词条+PFM选择后生成自创技能。
- 后续可继续推演**其他部位**（如轻功、内功），为该技能追加新部位。
- 每个部位独立选择词条（受该部位规则约束），PFM 也按部位分组。
- 所有部位的词条和 PFM 合并到同一个自创技能中。

**数据影响**：

```javascript
// 秘籍上记录多部位数据
{
    zc_positions: ["拳脚", "轻功"],     // 已推演的部位列表
    zc_words: {                         // 按部位分组的词条
        "拳脚": [500, 501, 510],
        "轻功": [504, 507],
    },
    zc_pfms: {                          // 按部位分组的PFM
        "拳脚": [{skill_id, pfm_key}, {skill_id, pfm_key}],
        "轻功": [{skill_id, pfm_key}],
    },
}
```

**PFM 上限放宽**：每部位 PFM 上限仍为 3 个，但多部位合计可超过 3 个。门派 PFM 限制按**全局**计算（整个自创技能最多 1 个门派 PFM）。

**技能装备**：多部位自创技能可同时装备到多个基础技能位（`can_enables` 包含所有已推演部位的 base skill）。

### 4.4 PFM 选择机制（核心新内容）

#### 4.4.1 可选PFM范围

从玩家已有技能中筛选：
- 技能等级 ≥ 3000
- 技能有 `pfm` 定义且非空
- 排除知识类技能 (SKILL_TYPES.KNOWLEDGE)

#### 4.4.2 PFM 武道书消耗

消耗取决于**来源技能的 grade**：

| 来源技能 grade | 每个PFM消耗武道书 |
|--------------|-----------------|
| 1 (白色)      | 1 本 |
| 2 (绿色)      | 2 本 |
| 3 (蓝色)      | 3 本 |
| 4 (紫色)      | 5 本 |
| 5 (金色)      | 8 本 |
| 6 (橙色)      | 12 本 |
| 7 (红色)      | 20 本 |

每部位最多选择 **3 个 PFM**，且不能重复选择同一个 PFM。多部位推演时，各部位 PFM 合并，合计可通过 3 个。

#### 4.4.3 门派PFM限制

自创技能可融合门派技能的 PFM，但有以下限制（**全局生效，跨部位计算**）：

- **最多融合 1 个门派 PFM**：整个自创技能中来自门派技能的 PFM 最多 1 个。
- **不可混合不同门派**：不能同时融合来自两个不同门派的 PFM。
- **不可融合多个同门派 PFM**：即使同一门派有多个符合条件的技能，也只能选择其中 1 个 PFM。
- 非门派（公共）技能的 PFM 不受此限制。

#### 4.4.4 PFM 数据格式

```javascript
// 服务端返回可选的PFM列表
pfms_available: [
    {
        skill_id: "kongmingquan",
        skill_name: "空明拳",
        skill_grade: 4,
        pfm_key: "pfm1",
        pfm_name: "乱拳",
        pfm_desc: "瞬间对敌人攻击6拳...",
        pfm_mp: 918,
        pfm_distime: 20000,
        cost: 5,  // 武道书消耗 (grade 4 → 5本)
    },
    // ...
]
```

#### 4.4.5 PFM 复制到自创技能

选定后，服务端将 PFM 定义深拷贝到自创技能的 `pfm` 对象中：

```javascript
// zc.js 中处理确认
var pfm_obj = {};
for (var i = 0; i < selected_pfms.length; i++) {
    var sel = selected_pfms[i];
    var src_skill = SKILL.get(sel.skill_id);
    var src_pfm = src_skill.pfm[sel.pfm_key];
    // 深拷贝PFM，调整 enable_skill 为自创技能的部位
    var copied = deepClone(src_pfm);
    copied.enable_skill = zc_position_base;  // 改为自创技能的部位
    copied.source_skill = sel.skill_id;      // 保留来源记录
    copied.source_pfm = sel.pfm_key;
    pfm_obj["pfm" + (i + 1)] = copied;
}
custom_skill.pfm = pfm_obj;
```

---

## 五、词条升级系统（核心新内容）

### 5.1 基础概念

推演完成后，每个选中的词条初始等级为 **1 级**。玩家可以消耗**潜能**单独升级每个词条。

- 词条等级没有上限
- 每升一级，该词条的属性加成按倍数增长
- 升级消耗随等级递增

### 5.2 属性计算公式

属性以**正常技能 addin 值为基准**，`word_level` 提供温和倍率加成。自创比同级普通技能高，但控制在合理范围（最大约 5~15 倍）。

#### 5.2.1 固定值 — 基础属性（category 0）

适用于：攻击、防御、命中、招架、闪避、气血上限、内力上限

```
base = floor(lv / 100 + 1) × (grade + 1) × 36      ← 与普通技能 addin 一致
value = floor(base × (1 + word_level / 15))
```

| word_level | lv=500, grade=3 | lv=500, grade=6 | 相对普通技能(grade=6) |
|------------|-----------------|-----------------|----------------------|
| 1 | 864 × 1.07 ≈ 924 | 1512 × 1.07 ≈ 1617 | 1.07× |
| 15 | 864 × 2 = 1728 | 1512 × 2 = 3024 | 2× |
| 50 | 864 × 4.33 ≈ 3744 | 1512 × 4.33 ≈ 6552 | 4.3× |
| 100 | 864 × 7.67 ≈ 6624 | 1512 × 7.67 ≈ 11592 | 7.7× |
| 200 | 864 × 14.33 ≈ 12384 | 1512 × 14.33 ≈ 21672 | 14.3× |

#### 5.2.2 固定值 — 后天属性（category 1）

适用于：身法、悟性、根骨、臂力

```
base = (1 + grade × 2) × 80                            ← 与普通技能 addin 一致
value = floor(base × (1 + word_level / 15))
```

| word_level | grade=3 | grade=6 | 相对普通技能(grade=6) |
|------------|---------|---------|----------------------|
| 1 | 560 × 1.07 ≈ 599 | 1040 × 1.07 ≈ 1112 | 1.07× |
| 15 | 560 × 2 = 1120 | 1040 × 2 = 2080 | 2× |
| 50 | 560 × 4.33 ≈ 2426 | 1040 × 4.33 ≈ 4506 | 4.3× |
| 100 | 560 × 7.67 ≈ 4293 | 1040 × 7.67 ≈ 7973 | 7.7× |
| 200 | 560 × 14.33 ≈ 8026 | 1040 × 14.33 ≈ 14906 | 14.3× |

#### 5.2.3 百分比词条（category 2/3/4）

百分比由分类决定 divisor，`word_level` 和 `grade` 共同驱动。使用 `1 + ...` 保底 1%。

```
value = 1 + floor(word_level × (1 + grade) / (divisor × 2))
```

| 词条 | 分类 | divisor | grade=1, wl=15 | grade=1, wl=50 | grade=6, wl=100 | grade=6, wl=200 |
|------|------|---------|----------------|----------------|-----------------|-----------------|
| 气血上限% | 2-高级 | 20 | 1+1=2% | 1+2=3% | 1+17=18% | 1+35=36% |
| 最终伤害% | 3-稀有 | 25 | 1+0=1% | 1+2=3% | 1+14=15% | 1+28=29% |
| 伤害减免% | 3-稀有 | 25 | 1+0=1% | 1+2=3% | 1+14=15% | 1+28=29% |
| 攻速% | 3-稀有 | 25 | 1+0=1% | 1+2=3% | 1+14=15% | 1+28=29% |
| 忽视防御% | 3-稀有 | 25 | 1+0=1% | 1+2=3% | 1+14=15% | 1+28=29% |
| 释放时间% | 3-稀有 | 25 | 1+0=1% | 1+2=3% | 1+14=15% | 1+28=29% |
| 暴击% | 4-特殊 | 30 | 1+0=1% | 1+1=2% | 1+11=12% | 1+23=24% |
| 冷却% | 4-特殊 | 35 | 1+0=1% | 1+1=2% | 1+10=11% | 1+20=21% |

> 对比: 普通技能"残暴之心"暴击+4%(lv=500)。自创暴击%在 wl=200 时约 24%，是普通技能的 6 倍，合理偏高。

#### 5.2.4 固定时间值词条（category 4）

```
value = floor(word_level × (grade + 1) × multiplier / 3)   （毫秒缩减）
```

| 词条 | multiplier | grade=1, wl=15 | grade=3, wl=50 | grade=6, wl=200 |
|------|-----------|----------------|----------------|-----------------|
| 攻速 | ×2 | 20ms | 133ms | 933ms |
| 释放时间 | ×10 | 100ms | 666ms | 4666ms |
| 冷却 | ×15 | 150ms | 1000ms | 7000ms |

#### 5.2.5 特殊被动词条

血杀之意：每级 0.05% 转化率（已下调）

```
value = word_level × 0.05   // 气血每少1% → 伤害+value%
```

| word_level | 转化率 | 效果（剩30%血时） |
|------------|--------|-------------------|
| 1 | 0.05% | 3.5% 最终伤害 |
| 10 | 0.5% | 35% 最终伤害 |
| 20 | 1.0% | 70% 最终伤害（≈血刀原版） |
| 50 | 2.5% | 175% 最终伤害 |
| 200 | 10.0% | 700% 最终伤害 |

### 5.3 升级消耗

**线性增长模型**：升级到第 n 级的消耗 = `base_pot_cost × n`。自创为后期内容，玩家潜能充裕，线性增长简洁合理。

```javascript
// 从等级 n-1 升到 n 的消耗
upgrade_cost(target_level) {
    return this.base_pot_cost * target_level;
}
```

示例（攻击，base=100）：

| 等级 | 本次消耗潜能 | 累计消耗 |
|------|-----------|---------|
| 1 | 100 | 100 |
| 5 | 500 | 1,500 |
| 10 | 1,000 | 5,500 |
| 20 | 2,000 | 21,000 |
| 50 | 5,000 | 127,500 |
| 100 | 10,000 | 505,000 |
| 150 | 15,000 | 1,132,500 |
| 200 | 20,000 | 2,010,000 |

各级词条 200 级累计消耗参考（按分类排列）：

| 分类 | 包含词条 | base | 200级单次 | 200级累计 |
|------|---------|------|----------|-----------|
| 0-基础 | 攻/防/命/招/闪 | 100 | 2万 | 201万 |
| 0-基础 | 气血上限/内力上限 | 150 | 3万 | 301万 |
| 1-后天 | 臂力/身法/悟性/根骨 | 200 | 4万 | 402万 |
| 2-高级 | 气血上限% | 200 | 4万 | 402万 |
| 3-稀有 | 最终伤害%/伤害减免%/攻速%/忽视防御%/释放时间% | 250 | 5万 | 502万 |
| 4-特殊 | 暴击%/冷却%/攻速/冷却/释放时间 | 300 | 6万 | 603万 |
| 特殊被动 | 血杀之意 | 500 | 10万 | 1005万 |

> 无等级上限，线性增长使后期玩家可持续投入但不会过度膨胀。

### 5.4 升级命令

```
zc upgrade <skill_id> <word_index>

服务端:
1. 获取自创技能: var sk = SKILL.get(skill_id);
   校验: sk.is_custom === true
2. 获取玩家技能数据: var sk_data = me.skills[skill_id];
3. 获取当前词条等级: var cur = sk_data.word_levels[word_index];
4. 计算消耗: var target = cur + 1; var cost = word.base_pot_cost * target;
5. 校验: me.pot >= cost
6. 扣除潜能: me.pot -= cost;
7. 等级+1: sk_data.word_levels[word_index] = cur + 1;
8. 重新计算属性: sk.release_prop(me, old_lv); sk.attach_prop(me, new_lv);
9. 通知前端更新
```

### 5.5 前端交互

**秘籍查看界面**（通过秘籍物品查看，显示词条等级和升级入口）：

```
┌──────────────────────────────────────────┐
│  <降龙十八掌秘籍>  grade 4               │
│  ─────────────────────────────────────  │
│  ◆ 攻击 Lv.15    攻击+3024   [升级 1500潜能] │
│  ◆ 防御 Lv.8     防御+2318   [升级 800潜能]  │
│  ◆ 臂力 Lv.10    臂力+1733   [升级 2000潜能] │
│  ◆ 暴击% Lv.12   暴击+2%     [升级 3600潜能] │
│  ─────────────────────────────────────  │
│  绝招:                                   │
│  【乱拳】(源自空明拳)                    │
│  【左右互搏】(源自空明拳)                │
│  ─────────────────────────────────────  │
│  [推演] [命名] [学习]                    │
└──────────────────────────────────────────┘
```

**注意**: 词条升级、推演等操作以秘籍物品为蓝本。已学习到身上的自创技能与普通技能显示方式一致，不显示词条等级和升级入口。

---

## 六、命令汇总

| 命令 | 格式 | 说明 |
|------|------|------|
| 购买 | `buy blank_book from <npc_id>` | 从朱熹购买空白秘籍 |
| `zc view` | `zc view <item_id>` | 查看自创秘籍详情 |
| `zc name` | `zc name <item_id>` | 给秘籍命名 |
| `zc deduce` | `zc deduce <item_id>` | 开始推演 → 选部位 |
| `zc select` | `zc select <item_id> <position>` | 选部位 → 进词条选择 |
| `zc confirm` | `zc confirm <item_id> <word_ids> <pfm_ids>` | 确认推演 |
| `zc study` | `zc study <item_id>` | 学习秘籍技能 |
| `zc upgrade` | `zc upgrade <skill_id> <book_id> <word_index> [级数]` | 升级词条(消耗潜能) |
| `zc addword` | `zc addword <book_id> <position> <word_index>` | 进阶推演：为已有技能增加词条(消耗武道书) |
| `zc addpfm` | `zc addpfm <book_id> <position>` | 进阶推演：为已有技能增加PFM(消耗武道书) |
| `zc replaceword` | `zc replaceword <book_id> <position> <old_index> [new_index]` | 替换词条(消耗1本武道书，保留原等级) |
| `zc words` | `zc words <skill_id> <book_id> [position]` | 查看/管理词条(支持按部位筛选) |

---

## 七、技能数据显示

### 7.1 查看技能详情

已学习的自创技能与普通技能显示方式一致，不显示词条等级：

`checkskill zc_xxx` 返回的描述包含：

```
<降龙十八掌> 自创 罕见武技
xxx所创造的武功

◆ 攻击+3456
◆ 防御+864
◆ 臂力+1600

绝招
【乱拳】(源自空明拳)
内力消耗：918  出招时间：3.1秒  冷却时间：20秒
瞬间对敌人攻击6拳，如果全部命中，最后一拳无视对方150防御。

【左右互搏】(源自空明拳)
内力消耗：918  出招时间：3.1秒  冷却时间：30秒
再次使用你之前使用的绝招攻击敌人...
```

词条等级、升级入口等仅在**秘籍物品**查看界面中显示（见 5.5）。

### 7.2 技能列表中的显示

- `grade`: 由词条数决定（不是从已有技能进阶来的，所以是全新的）
- 可使用 `is_custom: true` 标记在前端技能列表中显示"自创"标签
- word_levels 影响属性加成计算，但不影响 grade

---

## 八、进阶推演（第二期）

已有自创技能可通过进阶推演继续成长：

1. **增加词条**: 消耗武道书为已有技能增加新词条（grade+1，等级上限+1）
2. **增加PFM**: 消耗武道书为已有技能增加新的PFM（已有<3个时）
3. **替换词条**: 消耗武道书替换已有词条（洗练）
4. **替换PFM**: 消耗武道书替换已有PFM

---

## 九、技能融合（与现有 `lingwu2` 配合）

现有的 `lingwu2` 命令用于技能融合。自创技能可以作为融合的**目标**（将某技能融合到自创技能上）但不能作为融合的**源**（被融合掉）。

---

## 十、优先级与分期

### 第一期（当前）

1. 朱熹NPC + 购买空白秘籍
2. 空白秘籍基础操作（查看/锁定/丢弃/命名/学习）
3. 推演流程（选部位 → 选词条 → 消耗武道书确认）
4. PFM自选（从3000级技能中选最多3个，消耗武道书按来源grade计算）
5. 词条升级（消耗潜能，线性递增，无等级上限）
6. 自创技能在技能列表中显示与装备
7. 前端的推演对话框和词条升级面板

### 第二期（武神阶段完成后）

1. 恢复等级判定（需达到武神阶段）
2. 进阶推演（已有技能增加词条/PFM）
3. 词条洗练/替换
4. 自创技能传承给随从

---

## 十一、关键设计决策总结

| 决策 | 理由 |
|------|------|
| 命名在推演前 | 必须先命名才能推演，逻辑清晰，一次性完成 |
| grade = 词条数 | 词条数直接决定技能品质等级，每加一个词条升一级 |
| PFM 来源于3000级技能 | 体现玩家积累，自创不是凭空造，而是博采众长 |
| PFM 消耗按来源grade阶梯定价 | 高级技能的PFM更珍贵，消耗更多武道书合理 |
| 词条用潜能无限升级 | 给后期玩家持续消耗潜能的出口，无上限但有自然天花板 |
| 线性递增消耗 | 简单直观，玩家容易理解和规划 |
| 复用 PROPERTIES/slot 体系 | 已有完善的 addin 机制，自创技能天然兼容 |
| 技能ID用 `zc_` 前缀 | 与现有技能体系隔离，方便识别和管理 |
