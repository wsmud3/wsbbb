# 本地 ↔ 云端仓库比对与发布说明（2026-09-12）

本说明记录本次「与云端 `wsmud3/wsbbb` 比对差异 → 上传本地最新版本 → 更新日志」的实际范围、比对结果与验证边界。功能改动本身见 `readme.md` 的 2026-09-12 条目。

## 比对结果

- 比对对象：本地 `main` ↔ 云端 `origin/main`（`https://github.com/wsmud3/wsbbb.git`，比对前云端 HEAD = `a20e152`）。
- 本地领先 **1 个提交**：`83d8a65` `feat(skill): 武学等级上限扩展到 grade9（亮白/亮蓝/亮粉）`。
- 本地**没有**未提交的已跟踪改动；本地**没有**相对云端被删除的文件；云端所有文件在本地均有对应版本。
- 跟踪文件数：2566（云端）→ 2568（本地），新增两个前端构建产物（旧哈希产物保留，便于仍打开的页面继续加载）。
- 未跟踪文件：`syntax_fail.txt`（历史遗留的临时清单，仓库内没有任何脚本读写它），**不随本次发布上传**。

## 差异清单（`git diff origin/main..HEAD`）

| 文件 | 增/删 | 改动与作用 |
|---|---|---|
| `os/skill/skill.js` | +10/−4 | 新增 `SKILL.MAX_GRADE = 9`；`query_grade` 封顶 6 → 9；技能注册表 `SKILL[技能][grade]` 由 7 档扩为 10 档；`level_color`/`level_desc` 补 3 档（造化仙法/鸿蒙仙法/混沌仙法） |
| `os/item/obj.js` | +3/−1 | 物品/秘籍品阶色表补 0~9（秘籍 `grade` 直接取自技能 `grade`） |
| `world/cmd/skill/lingwu.js` | +1/−1 | 领悟面板 `GRADE_COLORS` 补 3 档 |
| `world/cmd/skill/zc.js` | +1/−1 | 自创武学 `ZC_COLORS` 补 3 档 |
| `world/obj/book/lt.js` | +1/−1 | 残页/秘籍 `GRADE_COLOR` 补 3 档 |
| `world/obj/zc/blank_book.js` | +1/−1 | 自创秘籍 `grade_color` 补 3 档 |
| `src/dialog/skills.js` | +3/−2 | 技能列表 `level_color`、`wrap_name` 色阶表扩到 10 项 |
| `src/dialog/packet.js` | +2/−1 | 背包 `levels` 色阶表新增 `hiw:7`/`hib:8`/`him:9` |
| `src/styles/global.css` | +15/−0 | 新增 `.grade7`/`.grade8`/`.grade9` 边框色（#FFFFFF/#0000FF/#FF00FF） |
| `www/assets/index-YlBYVsKC.js` | +2721 | 前端生产 bundle 重建（含上述前端改动） |
| `www/assets/index-DKZktP61.css` | +1 | 样式产物重建（含 `.grade7~9`） |
| `www/index.html` | +2/−2 | 入口指向新的哈希产物 |
| `readme.md` | +8/−0 | 2026-09-12 更新日志条目 |
| `docs/技能设计.md` | +1/−1 | grade 注释由 0~6 改为 0~9 |

## 本次未改动（避免误读）

- `lingwu` 进阶仍按原规则封顶 `grade5`；自创武学品质仍按「单部位最多词条数」封顶 `grade6`；真意仍排除 `grade ≥ 6` 的技能。
- `world/extends/data.js` 的 `DATA.book_values`、`world/obj/book/bc.js` 的 `COMBINED` 仍是 7 项（grade0~6）；若要用 grade7~9 技能产出秘籍，需要同步补齐，否则取值为 `undefined`。
- 未改动任何数值平衡、未开放新地图/新玩法、未调整存档格式。

## 可重复验证

- `npm run validate:systems`：本机通过（编码检查、真意/房间/系统完整性/装备重铸、山外山、后台面板与后台接口回归）。本次为**逐项断言全通过**，退出码 0。
- 前端产物一致性：`www/index.html` 引用的 JS/CSS 均存在；新 bundle 中确认包含 `levels:{wht:0,...,hiw:7,hib:8,him:9}`、`level_color:[...,hiw,hib,him]`、`wrap_name` 色阶表与 `.grade7/.grade8/.grade9` 样式。
- 云端一致性：推送前后用 `git ls-remote origin main` 核对远端 HEAD，并确认 `git status -sb` 不再显示 `ahead`。

## 验证边界（未做）

- 本机**没有 `node_modules`**（`better-sqlite3` 等依赖未安装），因此 `npm test` 中依赖数据库/HTTP/WS 的用例（如 `tools/test_release_safety.cjs`、`tools/test_external_fixes.cjs`）**未能本机执行**，也不代表线上验收结论。
- 未运行 `npm run build`（需要 Vite 依赖）；本次发布沿用仓库内已提交的构建产物，并已逐项核对产物内含改动。
- 未重启游戏服/Web 服务，未做线上压测与玩家客户端实测；grade7~9 需要技能文件显式定义 `this.grade` 才会实际出现。
